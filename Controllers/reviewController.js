var users = require("../models/Users");

// GET request - get reviews for an user by user id
view = (req, res) => {
    res.send("please implement!")
}

// POST request - post a review as an user by user id
post = (req, res) => {
    res.send("please implement!")
}

module.exports = {
    view,
    post,
}